// Stub — consumer de respuestas de saga vía Kafka (integración futura)
class SagaReplyConsumer {
  async start() {
    console.debug('[SagaReplyConsumer] No configurado');
  }
}

module.exports = { SagaReplyConsumer };
